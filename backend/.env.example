# CRITICAL: The public address of the deployed frontend (website).
# For production, use: https://chexn.com
# For local development, leave empty or use: http://localhost:5173
FRONTEND_URL=https://chexn.com

# CRITICAL: Your Stripe Test Secret Key (starts with sk_test_)
# Get this from Stripe Dashboard > Developers > API keys > Secret key
STRIPE_SECRET_KEY=sk_test_...

# CRITICAL: Your Stripe Test Webhook Signing Secret (starts with whsec_)
# Get this from Stripe Dashboard > Developers > Webhooks > Add endpoint > Signing secret
STRIPE_WEBHOOK_SECRET=whsec_...

# Path to your Firebase Admin SDK Service Account JSON file
# Default: ./firebase-admin-key.json (relative to backend folder)
# Can be absolute or relative path
FIREBASE_ADMIN_KEY_PATH=./firebase-admin-key.json

# Server port (optional, defaults to 5000)
PORT=5000

# CORS Origins (optional, defaults to localhost:5173 and FRONTEND_URL)
# Comma-separated list of allowed origins
# Example: http://localhost:5173,https://chexn.com,https://www.chexn.com
CORS_ORIGINS=
